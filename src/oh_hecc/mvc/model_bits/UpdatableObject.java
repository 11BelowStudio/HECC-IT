package oh_hecc.mvc.model_bits;

/**
 * An interface with an 'update()' method (for objects that are supposed to be updatable).
 */
public interface UpdatableObject {

    /**
     * This is an update method to update this object.
     */
    void update();
}
